import React from 'react';
import {CircularProgress} from "@mui/material";

function Loader() {
    return (
        <div>
            <CircularProgress/>
        </div>
    );
}

export default Loader;